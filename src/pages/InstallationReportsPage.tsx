import { AppLayout } from '@/components/layout/AppLayout';
import { InstallationReports } from '@/components/installation/InstallationReports';

const InstallationReportsPage = () => {
  return (
    <AppLayout>
      <InstallationReports />
    </AppLayout>
  );
};

export default InstallationReportsPage;
